# AI 夜巡 V3 方法说明

## 1. 三种分数不能混用

### 原始机会分

沿用夜巡报告里的 5 项评分，每项 0 或 1 分：

| 指标 | 判断问题 |
|---|---|
| 客户明确 | 能否说清楚谁会付钱？ |
| 7 天 Demo | 能否用现有能力在一周内做出可验证原型？ |
| 30 天收费 | 是否存在现实的首单路径？ |
| 复用资产 | 代码、数据、流程或品牌能否重复使用？ |
| 长期壁垒 | 是否存在数据、渠道、流程或品牌积累？ |

这个分数保留原始研究判断，但不再被当成事实证明。

### 证据可信度

可信度回答“现有来源足以支持多强的结论”：

- **高可信**：存在直接市场或使用证据，并在多个独立窗口重复出现。
- **中可信**：存在一手技术来源并多次出现，但付费证据不足。
- **低可信**：缺少来源、仅单次出现，或结论超出来源支持范围。

每条来源必须同时记录：

- 来源链接；
- 它支持的结论；
- 它不能支持的结论。

### 求职证据完成度

每条目标岗位线由 5 个明确能力项组成：

- 已有证据：20 分；
- 部分具备：10 分；
- 证据缺口：0 分。

因此 70 分表示“3 项已有证据、1 项部分具备、1 项缺口”，不是模型根据关键词猜测出的匹配率。

## 2. 重复信号

重复信号统计某个主题在最近 30 份去重报告中出现了多少次：

```text
重复次数 / 30 份报告
```

按“报告是否出现该主题”计数，不按单篇报告中的关键词频率计数。高重复只说明它值得持续观察，不自动等于存在付费需求。

## 3. 最新报告质量门

每份报告检查五项：

1. 是否包含生成时间；
2. 是否记录模型；
3. 是否包含可追溯来源链接；
4. 是否存在结构化机会项；
5. 是否给出明确下一步行动。

每项 20 分。缺失项会在“运行健康”视图中明确显示，不进行静默补分。

## 4. 当前决策

证据目录为每个主题记录一个人工可复核决策：

- **进入构建**：与现有资产高度相关，且下一步可以被验证。
- **继续验证**：信号持续，但仍缺任务成功率、用户或付费证据。
- **渠道实验**：先验证获客渠道，不先建设完整产品。
- **观察**：保留信号，不分配主要开发时间。
- **暂缓**：当前证据不足，或与目标不匹配。

决策不由原始评分单独决定。

## 5. 验证账本

进入构建或验证的方向必须写入 `data/decision_ledger.json`。每条记录至少包含：

- 开始日期与复核日期；
- 可被证伪的假设；
- 基线和当前结果；
- 成功标准与检查点；
- 是否已有外部证据；
- 继续、调整或停止的条件。

状态只允许使用：

- **planned**：已定义验证方式，尚未开始；
- **validating**：验证进行中；
- **validated**：达到预先写下的成功标准；
- **rejected**：证据不支持原判断；
- **stopped**：主动停止投入并保留原因。

被否定的判断不能从账本删除。这样可以避免夜巡只展示“后来证明正确”的案例，也能开始计算判断质量，而不只是统计报告数量。

## 6. 可复现构建

`tools/build_dashboard_data.py` 从以下输入生成 `data/dashboard.json`：

- `reports/latest.md`
- `reports/archive/*.md`
- `data/evidence_catalog.json`
- `data/career_profile.json`
- `data/decision_ledger.json`
- 本地夜巡分析目录的文件计数

构建过程会：

- 按报告正文 SHA-256 去重；
- 只解析正式机会区，不把 GitHub 周榜标题误当成商业机会；
- 计算最近 30 份报告的主题重复次数；
- 输出来源状态、质量检查、岗位证据、验证进度和凭据扫描结果。

## 7. 已知局限

1. 夜巡仍包含 LLM 生成判断，需要人工复核。
2. 数据源受网络、反爬和接口可用性影响。
3. 历史报告格式不完全一致，旧报告质量分可能偏低。
4. GitHub star、榜单排名和媒体报道不能替代客户访谈或付款记录。
5. 当前尚缺长期的预测命中率、外部用户留存和 A/B 实验数据。

V3 的目标不是假装消除这些局限，而是让它们可见、可审计、可逐步补齐。
